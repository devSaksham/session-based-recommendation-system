model:
  name: gru_linear
  embedding_dim: 128
  hidden_dim: 128
  dropout: 0.1
  tie_embeddings: false
training:
  batch_size: 256
  epochs: 10
  learning_rate: 0.001
  weight_decay: 1.0e-5
  grad_clip_norm: 5.0
  early_stopping_patience: 3
