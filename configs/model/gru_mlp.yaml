model:
  name: gru_mlp
  embedding_dim: 128
  hidden_dim: 128
  dropout: 0.1
  tie_embeddings: false
  mlp_hidden_dim: 128
training:
  batch_size: 512
  epochs: 5
  learning_rate: 0.001
  weight_decay: 1.0e-5
  grad_clip_norm: 5.0
  early_stopping_patience: 3
